# FunASR - 阿里达摩院语音识别
funasr>=1.3.1

# FunASR ONNX runtime (CPU 推理引擎)
funasr_onnx>=1.3.1

# ONNX 导出依赖（首次使用 ONNX 引擎时需导出模型）
onnx
onnxscript

# 音频录制
sounddevice>=0.4.6

# 数值计算
numpy>=1.24.0

# WebSocket
websockets>=11.0

# PyTorch (CUDA 12.x, funasr 引擎需要)
torch>=2.0.0
